# Micronaut Test JUnit 5

Micronaut Test JUnit 5 5.1.0 的 `@MicronautTest` 常用配置适配，发布坐标为 `micronaut.test:junit5:1`。`micronaut/test/junit5/Main.norm` 验证真实 Annotation 降级；`norm test` 封装 JUnit Platform 和 Micronaut Test Extension 生命周期，真实 DI 与 `@MockBean` 验收见 `MicronautTestBindingIntegrationTest`。
